extends ../base
include ../mixins/video

block content
    div.profile__data
        img(src="/"+user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        if video
            each video in user.videos
                +video(video)
        else 
            span.empty__message User video will be coming soon...